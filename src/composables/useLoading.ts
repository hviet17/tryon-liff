export const useLoading = ({local, initialValue} = {}) => {
    // const {isLoading: isGlobalLoading} = storeToRefs(useGlobalLoadingStore())
    // const isLocalLoading = ref(Boolean(initialValue))
    //
    // const isLoading = computed({
    //     get: () => local ? isLocalLoading.value : isGlobalLoading.value,
    //     set: (payload) => (local ? isLocalLoading : isGlobalLoading).value = payload
    // })
    //
    //
    // return {
    //     isLoading,
    //     isGlobalLoading,
    //     isLocalLoading,
    //
    // }
}
